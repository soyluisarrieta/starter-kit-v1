---
name: project-architecture
description: >
  Full-stack Laravel + React + Inertia architecture patterns.
  Trigger: Understanding project structure.
---

# Project Architecture - Skill

## Tech Stack

- **Backend**: Laravel 12, Spatie Permission, Fortify
- **Frontend**: React 19 with Compiler, Inertia v2
- **Routing**: Laravel Wayfinder (auto-generates TypeScript routes)
- **State**: Zustand stores
- **UI**: shadcn/ui + Tailwind CSS 4
- **Tables**: TanStack Table v8
- **Tooling**: Bun, Vite, TypeScript strict, ESLint, Prettier, Pint
- **Testing**: PHPUnit

## Project Structure

```text
laravel-starter-kit/
├── app/
│   ├── Actions/           # Fortify actions
│   ├── Concerns/          # Reusable traits
│   ├── Enums/             # Backed enums with metadata
│   ├── Http/
│   │   ├── Controllers/
│   │   ├── Middleware/
│   │   └── Requests/      # FormRequests with traits
│   ├── Models/
│   └── Providers/
├── resources/js/
│   ├── components/
│   │   ├── ui/            # shadcn/ui components
│   │   ├── commons/       # Shared components
│   │   └── layout/        # Layout components
│   ├── pages/             # Inertia pages
│   ├── layouts/           # Page layouts
│   ├── stores/            # Zustand stores
│   ├── hooks/             # Custom React hooks
│   ├── lib/               # Utils (cn, helpers)
│   ├── types/             # TypeScript definitions
│   └── routes/            # Auto-generated by Wayfinder
├── database/
│   ├── migrations/
│   ├── seeders/
│   └── factories/
└── docs/skills/           # AI coding skills
```

## Data Flow

```text
1. User Request
   ↓
2. Laravel Route (web.php)
   ↓
3. Controller
   ↓
4. Inertia::render('page', $data)
   ↓
5. React Page Component (resources/js/pages/)
   ↓
6. Render with layouts, components
```

## Response Flow

```text
1. React Component (form submit, button click)
   ↓
2. Inertia visit/router.post
   ↓
3. Laravel Controller
   ↓
4. Process (validation, DB, etc)
   ↓
5. Execute Inertia::flash message if success else to_route with error
   ↓
6. React receives updated props
```

### Component Composition

```tsx
// ✅ Page with layout
<AppLayout breadcrumbs={breadcrumbs}>
  <SettingsLayout>
    <DataTable data={data} columns={columns} />
  </SettingsLayout>
</AppLayout>
```

## Development Workflow

```bash
# Development (concurrent: server + queue + vite)
composer dev

# Linting
composer lint        # PHP (Pint)
bun run lint         # JS/TS (ESLint)
bun run format       # Prettier

# Testing
composer test
```

## Keywords

laravel, react, inertia, wayfinder, full-stack, architecture
